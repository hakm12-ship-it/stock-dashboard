# Render 배포 청사진 (Docker 단일 서비스)
# render.com → New → Blueprint → 이 저장소 선택하면 자동 인식.
services:
  - type: web
    name: stock-insight
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    autoDeploy: true
